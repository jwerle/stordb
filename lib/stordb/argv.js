
/**
 * Module dependencies
 */

let minimist = require('ext/minimist')

/**
 * Simple command line parser
 *
 * @api public
 * @param {String} argv
 */

module.exports = argv
export function argv (a) {
  return minimist(a);
}

